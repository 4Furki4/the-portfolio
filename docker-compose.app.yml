# Compatibility compose file for the Raspberry Pi production deployment.
# Prefer docker-compose.production.yml and docker-compose.development.yml for
# running both branches side by side.
include:
  - docker-compose.production.yml
